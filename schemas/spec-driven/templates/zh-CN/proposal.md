## 为什么

<!-- 说明该变更的动机。它解决了什么问题？为什么是现在？ -->

## 变更内容

<!-- 描述将发生哪些变化。明确指出新增能力、修改项或移除项。 -->

## 能力

### 新能力
<!-- 引入的能力。将 <name> 替换为 kebab-case 标识符（例如 user-auth、data-export、api-rate-limiting）。每项会生成 specs/<name>/spec.md -->
- `<name>`: <该能力覆盖内容的简要说明>

### 修改能力
<!-- REQUIREMENTS 发生变化的已有能力（不仅是实现细节）。
     仅当规格层面的行为改变时才列出。每项都需要一个增量规格文件。
     使用 openspec/specs/ 中已有规格名称。若无需求变化请留空。 -->
- `<existing-name>`: <哪些需求发生变化>

## 影响

<!-- 受影响的代码、API、依赖、系统 -->
