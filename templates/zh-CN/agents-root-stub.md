# OpenSpec 指南

这些说明面向在本项目中工作的 AI 助手。

当请求符合以下情况时，务必打开 `@/openspec/AGENTS.md`：
- 提到规划或提案（如 proposal、spec、change、plan）
- 引入新能力、破坏性变更、架构调整，或重大的性能/安全工作
- 请求含糊不清，需要在编码前确认权威规范

使用 `@/openspec/AGENTS.md` 了解：
- 如何创建和应用变更提案
- 规范格式和约定
- 项目结构和指南

保持此托管区块，以便 `openspec update` 能刷新这些说明。
